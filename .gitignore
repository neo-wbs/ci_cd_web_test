# Abhängigkeiten
node_modules/

# Build-Artefakte
dist/

# Lokale Umgebungsvariablen (nie einchecken!)
.env
.env.production

# Coverage-Reports
coverage/

# Editor
.vscode/
.DS_Store

# Falls Git .env bereits getrackt hat bevor du sie in .gitignore eingetragen hast. Dann hilft:
# git rm --cached .env
# git rm --cached .env.production
# git commit -m "remove .env files from tracking"